model Element {
    id           Int            @id @default(autoincrement())
    width        Int
    height       Int
    imageUrl     String
    MapElements  MapElements[]
    SpaceElement SpaceElement[]
}
